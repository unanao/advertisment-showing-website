/*
 * FIXME: 
 * 因为多数页面显示图片的个数是固定的 6
 * 想写一个固定缩放6个图片的js，直接包含可以使用的，
 * 但是没成功，但是写一个很有必要，所以保留，等待增加
 */

$(document).ready(function(){
    var mywidth = (document.body.offsetWidth) * 0.65 / 6;
	var myheight= width*3/4;
});