package com.bancai.dao;

public class UserActivationDAOTest {	
	public void testSave(){
		/*保存激活信息测试用例
		UserActivation activation = new UserActivation();
		activation.setCode("123456");
		activation.setTime(System.currentTimeMillis());
		activation.setUserId(1);
		new UserActivationDAO().save(activation);
		*/
	}
}