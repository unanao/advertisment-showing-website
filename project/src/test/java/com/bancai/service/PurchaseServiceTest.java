
package com.bancai.service;

import java.sql.Timestamp;

import junit.framework.TestCase;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;
/*
import com.bancai.dao.Purchase;

public class PurchaseServiceTest extends TestCase {	
	private Purchase purchase;
	private PurchaseService purchaseService;
	
	@Before
	public void setUp() throws Exception {
		purchase = new Purchase();
		purchaseService = new PurchaseService();
	}

	@After
	public void tearDown() throws Exception {
		super.tearDown();
	}

	@Test
	public void testAddBuyOrderInfo() {
		purchase.setEnterprise(20);
		purchase.setPackage_(1);
		purchase.setAmount(100d);
		purchase.setStartTime(new Timestamp(System.currentTimeMillis()));
		purchase.setEndTime(new Timestamp(System.currentTimeMillis() + 1000));
		purchase.setValid("valid");
		purchase.setTime(new Timestamp(System.currentTimeMillis()));
		
		purchaseService.addBuyOrderInfo(purchase);
	}
}
*/

